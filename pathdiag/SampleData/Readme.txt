Useful regression testing cases

Duplex - A real (local) duplex failure

PassDuplex - A real local path with no duplex failure and peak rate at awnd=4

overflow - A data set with internel sequence wraps (2^32 overflow)
